<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Email Verification | TripEase</title>

  <!-- Bootstrap + Font Awesome -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to right, #f8fbff, #e0f7fa);
      font-family: 'Poppins', sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0;
      padding: 1rem;
    }

    .verify-card {
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 2.5rem 2rem;
      width: 100%;
      max-width: 420px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.08);
      text-align: center;
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .verify-card h3 {
      font-weight: 700;
      color: #007bff;
      margin-bottom: 1.5rem;
    }

    .otp-input {
      font-size: 1.4rem;
      text-align: center;
      border-radius: 12px;
      padding: 0.7rem 1rem;
      width: 100%;
      border: 1px solid #ccc;
    }

    .btn-verify {
      margin-top: 1.5rem;
      border-radius: 25px;
      padding: 0.6rem 1.4rem;
      font-weight: 600;
    }

    .alert {
      font-size: 0.9rem;
      margin-top: 1rem;
      border-radius: 10px;
      padding: 0.75rem;
      background-color: #ffeaea;
      color: #d32f2f;
      border-left: 5px solid #e53935;
    }

    .resend-text {
      font-size: 0.9rem;
      margin-top: 1rem;
    }

    .brand-text {
      font-family: 'Pacifico', cursive;
      font-size: 2rem;
      color: #007bff;
      position: absolute;
      top: 20px;
      left: 30px;
      user-select: none;
    }
  </style>
</head>

<body>
  <div class="brand-text">TripEase</div>

  <div class="verify-card">
    <h3><i class="fas fa-envelope-open-text me-2 text-primary"></i> Verify Your Email</h3>
    <p class="mb-3">Enter the 6-digit OTP sent to your Gmail address.</p>

    <% if (error) { %>
      <div class="alert">
        <i class="fas fa-exclamation-circle me-1"></i> <%= error %>
      </div>
    <% } %>

    <form action="/verify-otp" method="POST">
      <input
        type="text"
        name="enteredOtp"
        maxlength="6"
        pattern="\d{6}"
        required
        class="otp-input"
        placeholder="Enter OTP"
      />
      <button type="submit" class="btn btn-primary btn-verify w-100 mt-3">Verify & Register</button>
    </form>

    <p class="resend-text text-muted mt-3">
      Didn't receive the OTP?
      <a href="/register" class="text-primary">Try again</a>
    </p>
  </div>
</body>
</html>
