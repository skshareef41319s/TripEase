<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Welcome | TripEase</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@400;600&display=swap" rel="stylesheet" />

  <!-- Feather Icons -->
  <script src="https://unpkg.com/feather-icons"></script>

  <!-- Smooth animation styles -->
  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(145deg, #e0f7fa, #ffffff);
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
      position: relative;
    }

    .intro-container {
      z-index: 2;
      text-align: center;
      opacity: 0;
      animation: fadeIn 1s ease-out forwards;
      animation-delay: 0.8s;
    }

    .logo svg {
      width: 80px;
      height: 80px;
      stroke: #007bff;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      fill: none;
      animation: drawLogo 2.5s ease forwards;
    }

    .logo-text {
      font-family: 'Pacifico', cursive;
      font-size: 2.8rem;
      color: #007bff;
      margin-top: 0.5rem;
      opacity: 0;
      animation: fadeIn 1s ease 2s forwards;
    }

    .tagline {
      font-size: 1.3rem;
      color: #333;
      font-weight: 500;
      width: 0;
      white-space: nowrap;
      overflow: hidden;
      border-right: 2px solid #007bff;
      margin: 0 auto 2rem;
      animation: typing 3.4s steps(30, end) 2.2s forwards, cursorBlink 0.8s step-end infinite 5.6s;
    }

    .start-btn {
      background: #007bff;
      color: white;
      padding: 0.8rem 2.2rem;
      font-size: 1rem;
      font-weight: 600;
      border: none;
      border-radius: 35px;
      cursor: pointer;
      opacity: 0;
      animation: fadeIn 1s ease 5.6s forwards, glow 2.5s ease-in-out infinite 6.2s;
    }

    .start-btn:hover {
      background: #0056b3;
      transform: scale(1.05);
    }

    /* Clouds */
    .clouds {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      overflow: hidden;
      z-index: 1;
    }

    .cloud {
      position: absolute;
      background: url('https://svgshare.com/i/12Xe.svg') no-repeat center;
      background-size: contain;
      width: 180px;
      height: 100px;
      opacity: 0.1;
      animation: moveCloud 60s linear infinite;
    }

    .cloud:nth-child(2) { top: 25%; left: -250px; animation-delay: 3s; }
    .cloud:nth-child(3) { top: 60%; left: -600px; animation-delay: 6s; }

    /* Animations */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(15px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes drawLogo {
      0% { stroke-dasharray: 0 100; }
      100% { stroke-dasharray: 100 0; }
    }

    @keyframes typing {
      from { width: 0; }
      to { width: 250px; }
    }

    @keyframes cursorBlink {
      50% { border-color: transparent; }
    }

    @keyframes glow {
      0%, 100% { box-shadow: 0 0 0px rgba(0,123,255, 0.4); }
      50% { box-shadow: 0 0 12px rgba(0,123,255, 0.3); }
    }

    @keyframes moveCloud {
      0% { transform: translateX(0); }
      100% { transform: translateX(120vw); }
    }

    @media (max-width: 500px) {
      .logo svg { width: 60px; height: 60px; }
      .logo-text { font-size: 2.2rem; }
      .tagline { font-size: 1.1rem; width: 190px; }
      .start-btn { font-size: 0.9rem; padding: 0.6rem 2rem; }
    }
  </style>
</head>

<body>
  <!-- Clouds Background -->
  <div class="clouds">
    <div class="cloud"></div>
    <div class="cloud"></div>
    <div class="cloud"></div>
  </div>

  <!-- Intro Content -->
  <div class="intro-container">
    <div class="logo">
      <svg viewBox="0 0 24 24">
        <path d="M2 16l20-8M2 12l10 2m-2 4l2 4m2-8l2 6m4-10l-4 2" />
      </svg>
      <div class="logo-text">TripEase</div>
    </div>
    <div class="tagline">Plan Smart. Travel Easy.</div>

    <form action="/login" id="startForm">
      <button type="submit" class="start-btn">Get Started</button>
    </form>
  </div>

  <!-- Feather icons -->
  <script>
    feather.replace();

    // 🌟 Play welcome sound
    const audio = new Audio("https://assets.mixkit.co/active_storage/sfx/1552/1552-preview.mp3");
    audio.volume = 0.7;
    window.addEventListener("DOMContentLoaded", () => {
      audio.play().catch(() => {}); // play on load if allowed
    });

    // Optional auto-redirect fallback
    setTimeout(() => {
      window.location.href = "/login";
    }, 10000);
  </script>
</body>
</html>
